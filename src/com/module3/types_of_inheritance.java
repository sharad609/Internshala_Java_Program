package com.module3;

public class types_of_inheritance {
	//Single Level Inheritance: when child class(subclass) is inherited from parent class(base class).
	//Multi Level Inheritance: when subclass,sub subclass,and so on,are inherited from parent class(base class).
	//Hierarchical Inheritance: when 2 subclass(child class) are inherited from same parent class(base class).
}
